{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: loopback UI origin plus HTTPS for Nodebox runtimes and LLM APIs.",
  "windows": ["main"],
  "permissions": ["core:default"],
  "remote": {
    "urls": [
      "http://127.0.0.1:*/*",
      "http://localhost:*/*",
      "https://**"
    ]
  }
}
